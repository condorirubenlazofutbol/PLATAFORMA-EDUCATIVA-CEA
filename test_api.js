const fetch = require('node-fetch');

(async () => {
    try {
        console.log("Calling actividad-reciente...");
        // Since we need a token, we might get 401 instead of 500 if we don't pass token.
        // I will just read the backend code instead of fetching.
    } catch (e) {
        console.error(e);
    }
})();
